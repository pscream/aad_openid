Create **.env** and put inside **REACT_APP_MS_CLIENT_ID**, **REACT_APP_MS_TENANT_ID**, **REACT_APP_MS_REDIRECT_URI** and **REACT_APP_API_URI** vars inside

#### Install packages:
`yarn`

## Running an application in dev mode
`yarn start`